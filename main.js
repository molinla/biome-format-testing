/**
 * I tried both pnpm or npm, but still will clean all codes.
 */


/**
 * following codes has lots of space, should be removed only left one after formating.
 * @type {number[]}
 */
const formatArray =                                                [1, 2, 3]
